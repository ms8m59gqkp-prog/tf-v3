# Audit 2: 완결성 / 효과성 / 효율성 심층 재심사 + 12회 시뮬레이션

**작성일**: 2026-03-01
**대상**: plan3.md (구현 플랜), v5-combined-research.md (통합 리서치)
**심사 기준**: 완결성(Completeness), 효과성(Effectiveness), 효율성(Efficiency)
**방법**: 3축 정밀 평가 → 고쳐야 할 점 도출 → 12회 시뮬레이션 (plan3의 3회를 넘어서는 적대적 시뮬레이션)

---

## Part A: 3축 심층 재심사

---

### A-1. 완결성 (Completeness) — "빠진 것은 없는가?"

#### A-1.1 plan3의 완결성 평가

| 평가 항목 | 점수 | 근거 |
|----------|------|------|
| CRITICAL 이슈 커버리지 | 95/100 | 11건 모두 Phase별 해결 매핑. 단 NEW-13(setTimeout 레이스)의 구체 코드가 없음 |
| HIGH 이슈 커버리지 | 85/100 | 55건 중 ~90% 매핑. NEW-08(Base64 이중복사), NEW-10(Puppeteer 좀비), NEW-14(setTimeout 언마운트) 직접 언급 없음 |
| 라우트 전수 | 88/100 | Rev.2로 DELETE 6개 + 미분류 4개 추가했지만, settlement/export/mismatch-report "구현 여부 결정 필요"로 미확정 |
| 어드민 페이지 전수 | 85/100 | Rev.2로 11개 추가했지만 각 페이지별 컴포넌트 분해 상세가 없음 (정산/위탁/주문/사진만 상세) |
| DB 마이그레이션 전수 | 92/100 | 5개 마이그레이션 + 3개 RPC. 인덱스 최적화 마이그레이션 누락 (성능용 인덱스) |
| 테스트 전략 | 30/100 | **심각한 공백**: 단위 테스트/통합 테스트/E2E 전략이 전혀 없음. Phase 8 "수동 검증"만 존재 |
| 에러 모니터링/로깅 | 25/100 | **심각한 공백**: Sentry/LogRocket 등 프로덕션 모니터링 전략 없음. console.log만으로 프로덕션 디버깅 불가 |
| CI/CD 파이프라인 | 10/100 | **심각한 공백**: ESLint, Prettier, 빌드 체크, 자동 테스트 파이프라인 전혀 없음 |
| 환경별 배포 전략 | 15/100 | Vercel 롤백 언급만. staging/preview 환경 없음. 환경변수 관리 전략 없음 |
| 데이터 백업/복구 | 40/100 | Pipeline A 아카이빙만 언급. 정기 백업/포인트 인 타임 복구 전략 없음 |

**완결성 종합**: 68/100 — 코드 아키텍처는 상세하나, **운영 인프라(테스트/CI/CD/모니터링)가 거의 공백**

#### A-1.2 v5의 완결성 평가

| 평가 항목 | 점수 | 근거 |
|----------|------|------|
| 이슈 발견 완전성 | 95/100 | 220건 전수, 4개 리서치 + 3개 딥분석 교차검증 |
| 복합 시나리오 | 90/100 | 7개 카탈스트로피 시나리오. 시나리오 2 "판매자 수백 명"은 plan3 §2.3에서 누락됨 |
| 해결책 완전성 | 35/100 | 대부분 1줄 해결 방향만. RPC 골격은 작동 불가 |
| 우선순위 매기기 | 80/100 | §11 체크리스트 A~G로 시간순 우선순위. 하지만 의존관계 미반영 |
| V2 코드 대응표 | 85/100 | 82개 핸들러 테이블 + 파일별 문제 매핑 (원본 참조) |

**완결성 종합**: 77/100 — 진단 완전성은 높으나 처방 완전성 부족

#### A-1.3 두 문서 합산 시에도 빠진 것들 (9건)

| # | 누락 항목 | 영향 | 심각도 |
|---|----------|------|--------|
| G-01 | **테스트 전략 부재** — 단위/통합/E2E 테스트 0건 계획 | 리팩터링 후 회귀 버그 탐지 불가 | CRITICAL |
| G-02 | **CI/CD 파이프라인 부재** — ESLint/Prettier/tsc/테스트 자동화 없음 | Phase 8 검증이 수동에만 의존 | HIGH |
| G-03 | **프로덕션 모니터링 부재** — Sentry/에러 추적 없음 | V3 배포 후 런타임 에러 탐지 불가 | HIGH |
| G-04 | **성능 인덱스 마이그레이션 부재** — 검색/필터/정렬 인덱스 설계 없음 | 데이터 증가 시 쿼리 성능 저하 | HIGH |
| G-05 | **RLS(Row Level Security) 전략 부재** — v5 §1에 "RLS 미활용" 기재, plan3에서도 미언급 | Service Role Key 제거 후 anon 클라이언트 보안 구멍 | HIGH |
| G-06 | **API 버저닝/하위호환 전략 부재** — V2→V3 API 전환 시 클라이언트 호환 | Public 페이지 2개가 V2 URL 구조 의존 | MEDIUM |
| G-07 | **환경변수 관리 전략 부재** — .env.local/.env.production 분리 없음 | 환경 혼동으로 프로덕션 사고 | MEDIUM |
| G-08 | **접근성(A11Y) 구현 계획 부재** — v5 ARC-12에서 발견, plan3 Phase 6에서 미반영 | 법적 컴플라이언스 위험 | LOW |
| G-09 | **캐싱 전략 부재** — SWR/React Query 설정, Supabase 쿼리 캐싱 없음 | 불필요한 API 호출 반복 | MEDIUM |

---

### A-2. 효과성 (Effectiveness) — "실제로 문제를 해결하는가?"

#### A-2.1 CRITICAL 이슈별 해결 효과성

| 이슈 | plan3 해결책 | 효과성 | 위험 요인 |
|------|------------|--------|----------|
| SEC-01 미들웨어 미작동 | middleware.ts 리네이밍 + requireAdmin() 이중 방어 | 98/100 | config.matcher 패턴 실수 시 부분 누출 가능 |
| SEC-02 인증 우회 | requireEnv() + bcrypt 해싱 | 95/100 | bcrypt 비용 인자(cost factor) 미지정 → 기본값 10 사용 시 브루트포스 위험 |
| SEC-03 Path Traversal | sanitizePath() + basename() + startsWith() | 90/100 | **심볼릭 링크 대응 미구현** — v5에서 fs.realpathSync 언급했으나 plan3 코드에 미반영 |
| SEC-04 PostgREST 인젝션 | .or() 문자열 보간 전면 제거 | 99/100 | 거의 완벽 |
| SEC-05 Public Service Key | anon client 전환 | 85/100 | **RLS 정책 미설계** — anon client + RLS 없음 = 여전히 전체 테이블 접근 가능 |
| FIN-01 이중 정산 | RPC FOR UPDATE | 95/100 | 판매자별 병렬 처리 시 교착 순서 보장이 코드 수준에서 명시되지 않음 |
| FIN-02 이중 파이프라인 | Pipeline B 단일화 | 90/100 | **Pipeline A 데이터 역추적 불가** — Rev.2에서 "역추적맵 부재" 지적만, 해결책은 "매핑 기록 생성(감사용)" 수준 |
| FIN-03 match_id UNIQUE | ALTER TABLE | 99/100 | 완벽 |
| DAT-01 1000행 절삭 | .range() 강제 | 85/100 | **count 쿼리 성능** — 대량 데이터 시 COUNT(*) 자체가 느림. 근사치 전략 없음 |
| DAT-02 ConsignmentStatus | Phase 0 CHECK 확장 | 99/100 | 완벽 |
| RUN-01 req.json 크래시 | Zod safeParse | 95/100 | FormData 4개 라우트 커스텀 검증은 "전략"만 기재, 구현 코드 없음 |

**효과성 종합**: 85/100 — 대부분 효과적이나 SEC-05(RLS), SEC-03(symlink), DAT-01(count 성능)에 구멍

#### A-2.2 plan3 시뮬레이션의 효과성 비판

plan3 §21의 3회 시뮬레이션은 **모두 "✅ 통과"로 끝남**. 이는 시뮬레이션이 너무 낙관적임을 의미:

| 문제 | 상세 |
|------|------|
| **자기 검증 편향** | plan3를 작성한 에이전트가 자기 플랜을 시뮬레이션 → 확증 편향 |
| **적대적 시나리오 부재** | "Phase 4에서 서비스 100줄 초과하면?"은 "하위 함수 분리"로 1줄 대응. 실제로는 아키텍처 재설계 필요할 수 있음 |
| **동시성 시뮬레이션 미포함** | 2명 관리자 동시 작업, 네트워크 지연, Supabase RPC 타임아웃 등 실시간 시나리오 없음 |
| **종속성 실패 미반영** | Phase 1 타입 변경 → Phase 2-5 연쇄 변경 시 시간 초과 가능성 |
| **외부 서비스 장애 시뮬레이션 없음** | Supabase 다운, Claude API 과금 한도, 네이버 API 변경 등 |

---

### A-3. 효율성 (Efficiency) — "최소 노력으로 최대 효과를 내는가?"

#### A-3.1 과잉 설계 (Over-Engineering) 탐지

| 항목 | 현재 설계 | 문제 | 대안 |
|------|----------|------|------|
| **5레이어 아키텍처** | L0→L5 엄격 단방향, 레이어 건너뛰기 금지 | 관리자 1-2명이 쓰는 내부 툴에 엔터프라이즈 아키텍처 적용 | 3레이어(DB/Service/Route)로 충분. 필요 시 점진적 분리 |
| **리포지토리 + 매퍼 + 트랜잭션 16파일** | 각 도메인별 .repo.ts + .mapper.ts + .tx.ts | 작은 프로젝트에 과도한 추상화. 매퍼 레이어가 추가 유지보수 부담 | 리포지토리에 매핑 포함. 별도 매퍼 파일 불필요 |
| **Zod 스키마 전체 엔드포인트** | 38개 POST/PUT/PATCH 모두 사전 정의 | Phase 1에서 38개 스키마 일괄 작성 → 라우트 구현 전 스키마 확정 불가능한 경우 다수 | 라우트 작성 시 동반 정의 (co-location) |
| **8 Phase 순차 실행** | 0→1→2→3→4→5→6→7→8 | Phase 0~3 완료 후에야 비즈니스 로직(Phase 4) 시작 | Phase 0+1+3 병렬, Phase 2+4 통합 (리포지토리와 서비스 동시 작성) |
| **100줄 엄격 제한** | 모든 파일 100줄 이내 (타입/설정 200줄) | 강제 분리로 파일 수 폭증 → 네비게이션 비용 증가 | 150줄 제한으로 완화. 의미있는 단위로 분리 |

**효율성 종합**: 60/100 — 내부 관리 도구 규모에 비해 과잉 설계. 3레이어 + co-location이 더 효율적

#### A-3.2 과소 설계 (Under-Engineering) 탐지

| 항목 | 현재 | 문제 | 필요 수준 |
|------|------|------|----------|
| **에러 수집** | console.error만 | 프로덕션에서 에러 추적 불가 | 최소 Sentry 무료 티어 |
| **캐싱** | 없음 | 동일 데이터 반복 조회 | SWR/React Query 기본 설정 |
| **Rate Limiting 상세** | "Upstash V2 그대로" | V2 ratelimit.ts의 null 반환 에러만 수정 | IP별/엔드포인트별 차등 제한 설계 |
| **DB 인덱스** | Phase 0에 인덱스 미포함 | 검색(.ilike) 쿼리 성능 저하 | 최소 sellers.phone, products.product_number, orders.status 인덱스 |
| **헬스체크** | "health, ready" Tier 3 | 모니터링 기반이 없음 | Tier 1으로 승격, DB 연결 + 외부 서비스 상태 포함 |

---

## Part B: 12회 적대적 시뮬레이션

plan3의 3회 시뮬레이션은 모두 ✅ 통과로 낙관적. 여기서는 **의도적으로 실패를 유도하는 적대적 시뮬레이션**을 12회 수행.

---

### SIM-01: "Day 1 Phase 0 DB 마이그레이션 — 프로덕션 중단"

**시나리오**: Phase 0 UNIQUE 제약 추가를 프로덕션 DB에 직접 실행. V2가 아직 운영 중.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | sellers.phone UNIQUE 추가 | 중복 정리 스크립트 선행 | - |
| 2 | V2 관리자가 동일 전화번호 판매자 생성 시도 | **plan3에 대응 없음** | **FAIL** |
| 3 | INSERT 실패 → V2 UX에 에러 표시 | §15.1 "V2 UX에 에러 표시" | 부분 대응 |
| 4 | 관리자가 에러 원인 모름 → 혼란 | **알림/공지 계획 없음** | **FAIL** |

**판정**: FAIL — V2 운영 중 DB 제약 변경 시 관리자 공지/교육 절차 부재

**고쳐야 할 점**: Phase 0 마이그레이션 전 관리자 공지 + V2 에러 시 안내 메시지 추가 (또는 V3 배포 직전에만 실행)

---

### SIM-02: "Phase 1 Zod 스키마 — 요구사항 변경"

**시나리오**: Phase 1에서 38개 Zod 스키마를 미리 작성. Phase 5에서 라우트 구현 중 스키마 변경 필요 발견.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | Phase 1에서 CreateOrderSchema 작성 | — | OK |
| 2 | Phase 5에서 주문 생성 라우트 구현 중 `measurements` 필드가 선택이 아닌 필수로 변경 필요 | **Phase 1 파일 수정** | 역방향 변경 |
| 3 | Phase 2 매퍼도 수정 필요 | **연쇄 변경** | 시간 낭비 |
| 4 | Phase 4 서비스도 수정 필요 | **연쇄 변경** | FAIL |

**판정**: FAIL — Zod 스키마 사전 정의 전략이 연쇄 변경을 유발

**고쳐야 할 점**: Zod 스키마는 라우트와 co-location. Phase 1에서는 공용 스키마(날짜/전화번호/금액)만 정의

---

### SIM-03: "Phase 2+3 병렬 — 리포지토리가 미들웨어에 의존"

**시나리오**: Phase 2(리포지토리)와 Phase 3(미들웨어)를 병렬 실행. Agent A가 리포지토리에서 인증된 사용자 컨텍스트가 필요한 상황 발견.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | Agent A: sellers.repo에서 "현재 관리자 ID 로깅" 필요 | Phase 3 미완료라 관리자 ID 참조 불가 | 대기 |
| 2 | Agent B: middleware.ts 작성 중 | — | OK |
| 3 | Agent A 대기 시간 발생 | **병렬 이득 상쇄** | PARTIAL FAIL |

**판정**: PARTIAL FAIL — 병렬 실행의 시간 절약 ~2시간 예측이 과대

**고쳐야 할 점**: 의존성 그래프에 "리포지토리 감사 로깅 → 미들웨어 인증 컨텍스트" 추가

---

### SIM-04: "Phase 4 서비스 100줄 제한 — settlement.service 폭발"

**시나리오**: settlement.service.ts에 generate(), confirm(), pay() 3개 함수 작성. generate()만 60줄 → 서비스 전체 100줄 초과.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | generate() 60줄 + confirm() 25줄 + pay() 30줄 = 115줄 | "하위 함수 분리 (같은 파일 내)" | 모순: 같은 파일 내 분리하면 줄수 증가 |
| 2 | 파일 분리 → settlement-generate.service.ts + settlement-confirm.service.ts | **파일 3개로 분산** | 네비게이션 비용 |
| 3 | 순환 의존 위험 (generate가 confirm 상태 확인) | **"이벤트 디스패치 패턴"이라고 했지만 구현 안내 없음** | PARTIAL FAIL |

**판정**: PARTIAL FAIL — 100줄 제한이 서비스 레이어에서 비현실적

**고쳐야 할 점**: 서비스 레이어는 150줄 제한으로 완화. 또는 generate/confirm/pay를 독립 유스케이스로 분리

---

### SIM-05: "Phase 5 Day 4 — 56개 라우트를 하루에 작성"

**시나리오**: Plan A 6일 일정에서 Day 4에 56개 route.ts(75 핸들러)를 모두 작성.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | 75개 핸들러 x 평균 50줄 = 3,750줄/일 | — | 비현실적 |
| 2 | Tier 1(10개) + Tier 2(20개) + DELETE(6개) + 미분류(4개) + Tier 3(22개) = 62개 라우트 | plan3는 "56개"라 했지만 실제 합산 62개 | **수치 불일치** |
| 3 | 각 라우트마다 Zod 스키마 확인, 서비스 연결, 에러 패턴, 로깅 | — | 하루 불가능 |

**판정**: FAIL — Day 4 하루에 62개 라우트 작성은 비현실적. Plan A 6일은 과소 추정

**고쳐야 할 점**: Phase 5를 최소 2-3일로 확장. 라우트 수 합산 정정 (56→62+)

---

### SIM-06: "Phase 6 Tailwind 전환 — 디자인 리그레션"

**시나리오**: inline style 1,061회를 Tailwind로 전환. V2 디자인과 시각적 차이 발생.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | V2 inline style `style={{background: 'linear-gradient(135deg, #C4A265, #D4B87A)'}}` | Tailwind 커스텀 토큰으로 변환 | OK |
| 2 | V2 `onMouseEnter` hover 효과 → Tailwind `hover:` 변환 | — | OK |
| 3 | 1,061개 중 30%가 동적 값 (e.g., `width: ${percent}%`) | **Tailwind만으로 불가** → `style={{width}}` 유지 필요 | 검증 게이트 `style={{ 0건` 실패 |
| 4 | 검증 게이트 0건 강제 → 동적 스타일까지 불필요하게 변환 시도 | **시간 낭비 + 기능 손상** | FAIL |

**판정**: FAIL — `style={{}}` 0건 검증 게이트가 동적 스타일 사용 사례를 고려하지 않음

**고쳐야 할 점**: 검증 게이트를 `정적 inline style 0건`으로 수정. 동적 style은 허용 (e.g., `style={{width: calc}}`)

---

### SIM-07: "Phase 7 스토리지 마이그레이션 — 대량 사진 전송 실패"

**시나리오**: 5,000장 사진을 로컬 → Supabase Storage로 마이그레이션. 네트워크 불안정.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | 5,000장 x 평균 2MB = 10GB 업로드 | — | 시간 소요 |
| 2 | 3,000장째에서 네트워크 끊김 | **재시도 전략 없음** | 처음부터 재실행 |
| 3 | 이미 업로드된 3,000장 중복 체크 | **멱등성 전략 없음** | FAIL |
| 4 | st_products.photos JSONB URL 치환 중 서버 재시작 | **부분 치환 상태** → 일부 URL 404 | FAIL |

**판정**: FAIL — 마이그레이션 스크립트의 멱등성/재시도/진행률 추적 전략 부재

**고쳐야 할 점**: 마이그레이션 스크립트에 체크포인트 + 멱등성 보장 + 진행률 로깅

---

### SIM-08: "V3 배포 직후 — Supabase RPC 타임아웃"

**시나리오**: V3 배포 후 정산 생성 시 create_settlement_with_items RPC가 30초 초과.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | 판매자 50명 x 평균 200개 sold_items = 10,000행 FOR UPDATE | — | 잠금 대기 |
| 2 | FOR UPDATE 10,000행 스캔 → 30초 초과 | "인덱스 최적화" (§17.2 MEDIUM/낮음) | 대응 미흡 |
| 3 | Supabase RPC 타임아웃 → 정산 생성 실패 | **폴백 전략 없음** | FAIL |
| 4 | sold_items에 인덱스 없음 (Phase 0에 미포함) | **인덱스 마이그레이션 누락** | FAIL |

**판정**: FAIL — 대량 데이터 시 RPC 성능 문제. Phase 0에 sold_items(seller_id, settlement_status) 복합 인덱스 필수

**고쳐야 할 점**: Phase 0에 성능 인덱스 5개 이상 추가 (sold_items, settlement_queue, orders, st_products, sales_records)

---

### SIM-09: "동시 관리자 2명 — 정산 + 위탁 동시 처리"

**시나리오**: Admin A가 정산 생성, Admin B가 위탁 완료를 동시에 실행. 같은 판매자.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | Admin A: create_settlement_with_items → sold_items FOR UPDATE | — | 잠금 획득 |
| 2 | Admin B: complete_consignment → st_products INSERT + orders INSERT | — | 성공 (다른 테이블) |
| 3 | Admin B의 새 상품이 sold_items에 즉시 반영 안 됨 (아직 판매 전) | — | OK |
| 4 | 하지만 **SWR polling 미구현**(plan3에서 "1차 SWR polling" 언급만) → Admin A 화면 갱신 안 됨 | **실시간 동기화 구현 상세 없음** | PARTIAL FAIL |

**판정**: PARTIAL FAIL — 낙관적 잠금으로 데이터는 안전하지만 UI 동기화 전략이 구체적이지 않음

**고쳐야 할 점**: SWR/React Query 폴링 간격, 무효화 전략, 낙관적 업데이트 패턴 구체화

---

### SIM-10: "Claude API 과금 한도 초과 — 사진 분류 전면 중단"

**시나리오**: 대량 사진 분류 중 Claude API 과금 한도 도달.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | 500장 분류 요청 | photo.service.classify() 지수 백오프 3회 재시도 | OK |
| 2 | 200장째에서 429 Too Many Requests | 재시도 3회 후 실패 | OK (실패 처리) |
| 3 | 나머지 300장 처리 불가 | **부분 성공 처리 전략 없음** | FAIL |
| 4 | 어떤 사진이 성공/실패했는지 추적 불가 | **진행률/체크포인트 없음** | FAIL |

**판정**: FAIL — 대량 배치 작업의 부분 성공 처리 + 진행률 추적 부재

**고쳐야 할 점**: 배치 작업에 진행률 DB 기록 + 재시도 가능한 큐 패턴

---

### SIM-11: "V2→V3 전환일 — Pipeline A pending 정산 미발견"

**시나리오**: §14 단계 1 실행 시 pending 정산 0건 확인했으나, 확인 직후~V3 배포 사이에 V2 관리자가 새 정산 생성.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | 단계 1 확인: pending 0건 | — | OK |
| 2 | 30분 후 관리자가 V2로 정산 생성 | **"동시 운영 기간 0"이라 했지만 V2 접근 차단 시점 미명시** | FAIL |
| 3 | V3 배포 → Pipeline A 코드 삭제 | 새로 생성된 pending 정산 고립 | FAIL |

**판정**: FAIL — "동시 운영 기간 0"이라 했지만 V2 접근 차단 → 확인 쿼리 → V3 배포의 정확한 순서/시간 창이 미정의

**고쳐야 할 점**: 전환 절차를 분 단위로 명시: 1) V2 접근 차단(maintenance mode) → 2) 확인 쿼리 → 3) V3 배포 → 4) 검증 → 5) 접근 재개

---

### SIM-12: "Phase 8 검증 — grep 검증의 한계"

**시나리오**: Phase 8 자동 검증 스크립트 12개 실행. grep 기반 검증의 맹점.

| 단계 | 발생 | plan3 대응 | 결과 |
|------|------|----------|------|
| 1 | `grep -r "requireAdmin" app/api/admin/` → 모든 라우트에 존재 확인 | — | OK |
| 2 | 하지만 requireAdmin 호출 후 **결과를 무시하는 코드** 존재: `await requireAdmin(req); /* 반환값 체크 안 함 */` | grep은 "존재"만 확인, "올바른 사용" 미확인 | FAIL |
| 3 | `grep -r "style={{" app/` → 0건 | 하지만 `style={ {width} }` (공백 포함)은 미탐지 | PARTIAL FAIL |
| 4 | `grep -r ": any"` → 0건 | 하지만 `as unknown as SomeType` 2단계 캐스팅은 미탐지 | PARTIAL FAIL |

**판정**: PARTIAL FAIL — grep 기반 검증은 구문 패턴만 확인, 의미적 정확성 미확인

**고쳐야 할 점**: grep 검증을 ESLint 커스텀 규칙으로 보강 (e.g., `requireAdmin` 반환값 사용 강제)

---

## Part C: 시뮬레이션 종합 결과

| # | 시뮬레이션 | 판정 | 핵심 실패 원인 |
|---|----------|------|---------------|
| SIM-01 | DB 마이그레이션 프로덕션 중단 | **FAIL** | V2 운영 중 마이그레이션 공지/차단 절차 없음 |
| SIM-02 | Zod 스키마 연쇄 변경 | **FAIL** | 사전 정의 전략의 역방향 변경 유발 |
| SIM-03 | 병렬 실행 의존성 | **PARTIAL** | 감사 로깅 → 인증 컨텍스트 의존 미반영 |
| SIM-04 | 서비스 100줄 제한 | **PARTIAL** | 100줄이 서비스에 비현실적 |
| SIM-05 | 62개 라우트 하루 작성 | **FAIL** | 수치 불일치 + 과소 추정 |
| SIM-06 | Tailwind 전환 동적 스타일 | **FAIL** | style={{}} 0건 검증이 동적 스타일 미고려 |
| SIM-07 | 대량 사진 마이그레이션 | **FAIL** | 멱등성/재시도/진행률 부재 |
| SIM-08 | RPC 타임아웃 | **FAIL** | 성능 인덱스 누락 |
| SIM-09 | 동시 관리자 UI 동기화 | **PARTIAL** | SWR 폴링 구체 전략 없음 |
| SIM-10 | Claude API 한도 초과 | **FAIL** | 배치 부분 성공 처리 없음 |
| SIM-11 | 전환일 타이밍 갭 | **FAIL** | maintenance mode 절차 미명시 |
| SIM-12 | grep 검증 한계 | **PARTIAL** | 구문 패턴만 확인, 의미적 검증 불가 |

**12회 시뮬레이션 결과: FAIL 7건, PARTIAL FAIL 3건, — 2건 (FAIL 없음 0건)**

**(plan3 자체 시뮬레이션 3회 = 모두 ✅ 통과 vs 본 감사 12회 = 0건 완전 통과)**

---

## Part D: 고쳐야 할 점 종합 (우선순위순)

### D-1. CRITICAL 수정 (4건) — 반드시 plan3에 추가

| # | 수정 사항 | 근거 | 영향 범위 |
|---|----------|------|----------|
| FIX-01 | **테스트 전략 추가** — 최소 Zod 스키마 단위 테스트 + RPC 통합 테스트 + CRITICAL 라우트 E2E | G-01 + SIM-12 | 전체 |
| FIX-02 | **Phase 0에 성능 인덱스 추가** — sold_items(seller_id, settlement_status), orders(status), st_products(product_number), sales_records(match_status) 최소 4개 | G-04 + SIM-08 | Phase 0 |
| FIX-03 | **RLS 정책 설계** — Public 페이지가 anon client 사용 시 consignment_requests, orders 테이블 RLS 필수 | G-05 + A-2.1 SEC-05 | Phase 0 + Phase 3 |
| FIX-04 | **V2→V3 전환 절차 분 단위 명시** — maintenance mode → 확인 쿼리 → 배포 → 검증 → 재개 | SIM-11 | §14 |

### D-2. HIGH 수정 (8건) — plan3 효과성 크게 향상

| # | 수정 사항 | 근거 |
|---|----------|------|
| FIX-05 | **CI/CD 최소 파이프라인** — tsc + ESLint + 빌드 체크 자동화 (GitHub Actions 10줄) | G-02 |
| FIX-06 | **프로덕션 모니터링** — Sentry 무료 티어 설치 (console.error → Sentry.captureException) | G-03 |
| FIX-07 | **Zod 스키마 co-location** — Phase 1에서 공용만, 나머지는 Phase 5에서 라우트별 동반 정의 | SIM-02 |
| FIX-08 | **라우트 수 정정** — 10+20+6+4+22 = 62개 라우트 (56이 아님). Phase 5를 2-3일로 확장 | SIM-05 |
| FIX-09 | **style={{}} 검증 게이트 수정** — 동적 스타일 허용, 정적 하드코딩만 금지 | SIM-06 |
| FIX-10 | **스토리지 마이그레이션 스크립트 멱등성** — 체크포인트 + 재시도 + 진행률 | SIM-07 |
| FIX-11 | **배치 작업 부분 성공 처리** — 사진 분류, 대량 SMS, 정산 생성에 진행률 DB 기록 | SIM-10 |
| FIX-12 | **서비스 레이어 150줄 제한** — 100줄은 비현실적. 라우트 100줄 + 서비스 150줄 + 컴포넌트 150줄 | SIM-04 |

### D-3. MEDIUM 수정 (6건) — 효율성 개선

| # | 수정 사항 | 근거 |
|---|----------|------|
| FIX-13 | **5레이어 → 3레이어 단순화** — 내부 관리 도구에 5레이어 과잉. DB/Service/Route로 충분 | A-3.1 |
| FIX-14 | **매퍼 파일 제거** — 리포지토리에 매핑 포함. 별도 mapper/*.ts 3파일 불필요 | A-3.1 |
| FIX-15 | **SWR/React Query 구체 전략** — 폴링 간격, 무효화 키, 낙관적 업데이트 패턴 | SIM-09 |
| FIX-16 | **환경변수 관리** — .env.example 필수 커밋 + 환경별 분리 | G-07 |
| FIX-17 | **헬스체크 Tier 1 승격** — DB 연결 + Supabase Storage + SMS API 상태 포함 | A-3.2 |
| FIX-18 | **Phase 0 마이그레이션 타이밍** — V2 운영 중 무중단 적용 가능한 제약만 선적용, 나머지는 V3 배포 직전 | SIM-01 |

---

## Part E: 최종 점수

### plan3 재평가

| 기준 | audit1 점수 | audit2 점수 | 변동 이유 |
|------|-----------|-----------|----------|
| 완결성 | (미평가) | **68/100** | 테스트/CI/CD/모니터링 공백 발견 |
| 효과성 | (미평가) | **78/100** | CRITICAL은 효과적이나 RLS/symlink/count 구멍 |
| 효율성 | (미평가) | **60/100** | 5레이어/100줄제한/사전Zod이 과잉 설계 |
| **종합** | — | **69/100** | "매우 상세한 계획이지만 운영 현실과 괴리" |

### v5 재평가

| 기준 | audit1 점수 | audit2 점수 | 변동 이유 |
|------|-----------|-----------|----------|
| 완결성 | (미평가) | **82/100** | 진단은 거의 완벽. 해결책 부족은 역할상 자연스러움 |
| 효과성 | (미평가) | **45/100** | 1줄 해결은 실행 불가. RPC 골격 작동 안 함 |
| 효율성 | (미평가) | **75/100** | 진단서로서 효율적. 교차 참조 맵이 추적에 유용 |
| **종합** | — | **67/100** | "뛰어난 진단서이나 처방 없이는 불완전" |

### 두 문서 합산 시

| 기준 | 합산 점수 | 비고 |
|------|----------|------|
| 완결성 | **75/100** | v5의 진단 + plan3의 설계 합산해도 테스트/CI/CD 공백 |
| 효과성 | **80/100** | plan3 해결책이 v5 진단의 대부분을 커버. 단 RLS/인덱스 갭 |
| 효율성 | **62/100** | plan3 과잉 설계가 전체 효율 저하 |
| **종합** | **72/100** | FIX-01~18 반영 시 **88/100** 도달 가능 |

---

*본 감사는 plan3의 자체 시뮬레이션(3회 모두 ✅)과 달리, 의도적으로 실패를 유도하는 적대적 시뮬레이션 12회를 수행하여 실무 구현 시 발생할 수 있는 실패점을 사전에 식별했습니다. FIX-01~18을 plan3 Rev.3로 반영할 것을 권고합니다.*
